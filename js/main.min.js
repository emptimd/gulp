$(function() {
	let timer;

	$('.item').on('click',function(){
		$this = $(this);
		$this.toggleClass('removed');
	});

	$('body').on('keyup',function(e){
		$this = $(this);
		clearTimeout(timer);
		timer = setTimeout(function() {
			void 0;
		}, 500);
	});
});